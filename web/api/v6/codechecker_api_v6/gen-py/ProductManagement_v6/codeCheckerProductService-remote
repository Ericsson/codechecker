#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.11.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from ProductManagement_v6 import codeCheckerProductService
from ProductManagement_v6.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  string getPackageVersion()')
    print('  bool isAdministratorOfAnyProduct()')
    print('  Products getProducts(string productEndpointFilter, string productNameFilter)')
    print('  Product getCurrentProduct()')
    print('  ProductConfiguration getProductConfiguration(i64 productId)')
    print('  bool addProduct(ProductConfiguration product)')
    print('  bool editProduct(i64 productId, ProductConfiguration newConfiguration)')
    print('  bool removeProduct(i64 productId)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = codeCheckerProductService.Client(protocol)
transport.open()

if cmd == 'getPackageVersion':
    if len(args) != 0:
        print('getPackageVersion requires 0 args')
        sys.exit(1)
    pp.pprint(client.getPackageVersion())

elif cmd == 'isAdministratorOfAnyProduct':
    if len(args) != 0:
        print('isAdministratorOfAnyProduct requires 0 args')
        sys.exit(1)
    pp.pprint(client.isAdministratorOfAnyProduct())

elif cmd == 'getProducts':
    if len(args) != 2:
        print('getProducts requires 2 args')
        sys.exit(1)
    pp.pprint(client.getProducts(args[0], args[1],))

elif cmd == 'getCurrentProduct':
    if len(args) != 0:
        print('getCurrentProduct requires 0 args')
        sys.exit(1)
    pp.pprint(client.getCurrentProduct())

elif cmd == 'getProductConfiguration':
    if len(args) != 1:
        print('getProductConfiguration requires 1 args')
        sys.exit(1)
    pp.pprint(client.getProductConfiguration(eval(args[0]),))

elif cmd == 'addProduct':
    if len(args) != 1:
        print('addProduct requires 1 args')
        sys.exit(1)
    pp.pprint(client.addProduct(eval(args[0]),))

elif cmd == 'editProduct':
    if len(args) != 2:
        print('editProduct requires 2 args')
        sys.exit(1)
    pp.pprint(client.editProduct(eval(args[0]), eval(args[1]),))

elif cmd == 'removeProduct':
    if len(args) != 1:
        print('removeProduct requires 1 args')
        sys.exit(1)
    pp.pprint(client.removeProduct(eval(args[0]),))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
